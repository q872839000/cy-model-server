fastapi>=0.110.0
uvicorn>=0.24.0
loguru>=0.7.0
orjson>=3.9.0
prometheus-fastapi-instrumentator>=6.1.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
PyYAML>=6.0

# Model runtime
torch
transformers>=4.36.0
sentence-transformers>=2.2.2

# RAG / Vector DB (optional: only needed if you use Milvus-backed RAG endpoints)
pymilvus>=2.5.0

# Optional (Linux only; Windows is not supported by vLLM)
# vllm
